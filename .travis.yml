dist: "bionic"
env:
   - CPPFLAGS=--coverage

install:
  - sudo bash bootstrap.sh

script:
  - sudo docker-compose up -d mysql redis
  - sudo docker ps
  - sudo docker-compose run greencandle-unittest

deploy:
  -provider: script
   script: echo "I should not see this in a branch"
   on:
     tags: true
